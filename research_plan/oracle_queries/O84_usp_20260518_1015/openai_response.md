# openai response (gpt-5) — 156s

Q1 — Strongest survivable USP
- USP-1: One 2‑transistor CMOS body‑state cell as a multi‑function analog primitive at 130 nm, silicon‑anchored.
  - What is actually shown and survives audit: (i) calibrated DC to measured “Mario” target (Id_pk 4.21–4.45 mA vs 4.8 mA, ±0.06 dec across 4 biases); (ii) leaky integrate‑and‑fire with autonomous self‑reset on a pulse (V6 PASS, t_reset ≈ 41 ns) and integrate behavior (V8 PASS); (iii) free oscillation via a physical Hopf regime with period 420 ns within a physically valid Vb window and preserving the same Id_pk calibration (z477c); (iv) intrinsic device noise usable as a stochastic primitive with low cross‑cell correlation (K2 audit not triggered). All modes come from the same compact cell class (2T body‑state device), selected by bias, not a change of macro‑architecture. No rival can claim “memory + LIF + Hopf oscillator + stochastic primitive” from a single 2‑device cell at 130 nm with a silicon‑anchored model.
- USP-2: A transistor‑level differentiable physics port (IFT‑based) that is DC‑identical to BSIM4 and supports stable local learning on the physical dynamics.
  - What survives: IFT sign bug fixed; gradients verified; DC/transient value path bit‑identical under no_grad; GPU batching infra proven. Using this model, a physically constrained EP‑style rule (“EP‑FIX”: β‑cosine + random‑sign nudges + VG1‑nudge + early‑stop) yields 90.83±2.60% MNIST with near‑zero drift. Competing platforms do not offer a transistor‑accurate differentiable model tied to a demonstrated local learning rule on a CMOS body‑state device.

Q2 — Reviewer attack vectors and one‑experiment pre‑empt
- USP-1 attack (first and fiercest): “Are these modes truly from the same, unchanged cell, or are you swapping topologies/adding hidden state to get the oscillator?”
  - Pre‑emptive single experiment: Same‑cell, bias‑only “mode atlas” on silicon. Pick one die/cell; sweep a 2‑D grid over {VG1,Vd} (body‑leak fixed), and for each bias point record: (a) quiescent vs pulse‑evoked LIF with self‑reset (ISI, Vb recovery), (b) free‑oscillation (sustained AC with period; spectral peak), (c) noise statistics at rest (PSD, entropy rate). Deliver (i) one‑cell phase diagram labeling LIF, Hopf, noise regimes; (ii) overlay with IFT‑model prediction; (iii) replicate on 8–16 cells to show reproducibility and period spread. Pass if all three regimes occur on the same cell via bias only and period ≈ 420 ns ±20%.
- USP-2 attack (first and fiercest): “EP‑FIX is not EP (no Lyapunov); is the 91% just a small‑data/seed fluke?”
  - Pre‑emptive single experiment: Full‑MNIST (60k/10k) EP‑FIX, 5 seeds, ablation and alignment. Train with the IFT pyport; report test accuracy distribution, drift, and alignment cosines between EP‑FIX update and true gradient (from backprop through the pyport) at matched checkpoints; ablate β‑cos, random‑sign, and VG1‑nudge to show each is necessary/sufficient for stability. Pass if median test ≥90%, drift ≤1 pp, alignment cosine >0.3 for >80% of batches, and ablations degrade as predicted.

Q3 — Death sentence experiments (if they fail)
- DS-1 (cheapest): Full‑MNIST EP‑FIX robustness (compute‑only; pending).
  - Design: 5–10 seeds, 60k/10k, same hyperparams; report test, drift, update‑alignment. Kill criterion: median test <88% or recurrent collapse (drift <−5 pp) or alignment near zero across runs. Consequence: the “physical local learning” pillar falls; story shrinks to “device + differentiable modeling” only.
- DS-2 (moderate): Same‑cell, bias‑only multi‑mode silicon demo (single die; moderate lab time; pending).
  - Design: Mode atlas on one physical cell confirming pulse‑LIF with self‑reset, free Hopf oscillation, and stochastic rest noise, all via bias changes only; replicate across ≥8 cells. Kill criterion: inability to realize at least LIF+Hopf on the same cell without topology changes, or oscillation only with non‑physical biases. Consequence: the “one 2T cell = multi‑function primitive” USP collapses.
- DS-3 (expensive): 16×16 mismatch/correlation matrix on real die (needs Sebas die; pending).
  - Design: Measure noise cross‑correlation and mode reachability across the array under shared bias. Kill criterion: mean |corr| ≥0.3 or large swaths of cells cannot enter one of the claimed regimes under any safe bias. Consequence: both “stochastic primitive” and “bias‑programmable multi‑function” claims lose generality; modeling claims become non‑actionable due to lack of array‑level robustness.
