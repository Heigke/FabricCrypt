# grok response (grok-4-latest) — 11s

**Q1**  
The single most likely un-audited fragility is **hyperparameter/seed-path sensitivity in z481’s rescue stack** (β-cos + random-sign + VG1-nudge + early-stop). The 4-seed mean of 90.83±2.60% already shows one seed required early-stop rescue after single-batch blow-up; the reported drift recovery (+0.44 vs –11.75) is therefore conditional on that intervention.  

Exposing experiment: re-run the identical z481 pyport code for 10–12 fresh seeds with **early-stop disabled** (or with a fixed 50-epoch cap) while keeping all other fixes. Any collapse back toward the original 75±16% drift or >3 pp seed variance kills the “stable physical EP-FIX” claim.

**Q2**  
Formally retire “differentiable limit-cycle reservoir” from the v4.6 brief (or relegate it to a one-sentence MG-only caveat). z479 (NARMA-10 worse than baseline), z482 (mean spike-synchrony 0.0016, all gates fail), and the prior ERvMESH killshot already demonstrate that coupled or general FHN reservoirs add no temporal credit beyond a linear readout on MG.  

A reviewer might accept an explicit “MG-class chaotic forecasting only” scope note, but it still dilutes the O84-endorsed USP-1 (multi-function 2T cell via bias-programming alone). Keeping the broader claim invites “why mention a dead direction?” pushback; cut it.

**Q3**  
Rank by paper-impact-per-hour under current constraints (zgx free, ~24–48 h, Sebas unavailable):

1. **(b)** bias-programmable mode-atlas in pyport — directly realises the O84 3-way USP (“same calibrated cell: LIF + V6 reset 40 ns + V7 Hopf 420 ns + noise + EP-FIX”). One clean figure set cements the thesis; highest leverage.

2. **(a)** DS-1 full-MNIST EP-FIX 5–10 seeds + ablation + alignment-cosine — necessary validation of z481 but lower marginal impact once the USP framing is locked.

3. **(c)** GPU 16×16 mode-atlas variance sweep — useful DS-3 surrogate but only after (b) proves the bias-only multi-function story.

4. **(d)** revisit-NES-GD — lowest priority; K2 audit is secondary once peripheral energy claims are already dead.

Skip (e); none of the listed items outrank the USP demo.
