# grok response (grok-4-latest) — 59s

### Q1. Fragility audit on the D1 84.45% result

You're fooling yourselves with this "statistical tie" narrative—it's a classic case of over-claiming marginal results to fit a success story. The 84.45 ± 0.58% on d115 is 0.20 pp below the Poisson baseline (84.65 ± 0.72%), and zero cells hit the pre-registered ambitious gate (>84.65% strict, non-overlapping CI). Citing d050's CI upper bound (0.850) overlapping N1b's mean as a "tie" is cherry-picking; the lower bound (0.835) dips below, and with only 4 seeds per cell, variance is understated. All top cells cluster at V_G2=0.35 V (log ~01:10: "All top 8 cells at V_G2=0.35 V"), the exact edge of the pre-MEP surrogate's axis [0,0.45], screaming selection bias—you swept a 320-cell grid but highlight the boundary where the model might extrapolate favorably. The "broad plateau 5-14 fF" (max at 8 fF) is over-claimed triangulation; it's just sweep peaks aligning with prior assumptions from slides 11/21, not independent validation—PMP-9's transient match at 8 fF is circular if the sweep was tuned to it.

The V_G2=0.35 V edge effect is likely a surrogate artifact, not real silicon physics. The pre-MEP surrogate (z271) cuts off at 0.45 V, and MEP-2 extends to 0.60 V with non-convergence at high V_G2 (>0.45 V, log ~01:42: "Non-conv at high V_b + high V_G2"), suggesting instability beyond 0.35 V. Top performance pins to this edge because the surrogate overestimates productive dynamics there—real physics might show saturation or drop-off, as hinted by clip rate=20% in d115 (at V_G1 domain edge).

The 4-seed bootstrap CI on 1000/2000-image subsets is nowhere near tight enough for "matches Poisson." Subsets are tiny (half the test set?), inflating CI optimism—d115's ±0.58% is tight but on limited data; full 10k-image eval could widen it. Worse, you forgot to bootstrap jointly over Poisson encoding randomness and weight randomness. Log ~01:10 implies seeds vary weights, but Poisson spikes are re-generated per run? If not joint, CIs ignore encoding variance, underestimating error—classic omission in SNN evals, fooling you into thinking it's "viable."

Single experiment to falsify "NS-RAM is a viable rate-coded SNN input neuron": Re-run D1 sweep with MEP-2's extended surrogate (V_G2 to 0.60 V), but enforce strict no-clip (≤1%) and full 10k-test bootstrap over 10 seeds jointly randomizing weights and Poisson encoding. If peak drops below 83% or shifts away from 0.35 V with high rail/clip, it breaks viability—exposes edge artifact and variance fragility.

### Q2. MEP-1 asymmetry — is it physics or numerics?

This "MIXED RESULT" is a mess—trilinear interp (quadrilinear, actually, per mep1_summary.json) regressing 6/16 cells by up to -67.96 pp (d276: 77.74% → 9.78%) while boosting others +10.31 pp (d047: 9.55% → 19.86%) screams numerical artifact, not physics. You're deluding yourselves with the pre-registered PASS gate (±1 pp on d115, max improve ≥0.5 pp), ignoring the catastrophic regressions concentrated at dt ≥5e-7 s and low-mid V_G2 (e.g., d276 at V_G2=0.15, dt=5e-7; d258 at 0.05, dt=1e-7; d282 at 0.15, dt=1e-6). These aren't "suspicious asymmetries" (log ~02:00)—they're evidence of broken modeling, where interp choice flips accuracy from decent to random-guessing (10% baseline).

Most likely is hypothesis b: trilinear interpolation introduces spurious values across V_b discontinuities. The surrogate is a 4D grid (V_G1, V_G2, V_d, V_b), and physics has sharp nonlinearities in V_b (e.g., impact ionization thresholds, body charging cliffs). Trilinear averages across these, injecting unphysical currents/Iii at query points—evident in regressions at larger dt (slower integration amplifies interp errors) and low V_G2 (weaker gate control, more sensitive to V_b noise). Hypothesis a (NN floor-snap artifact) is less likely; NN might underperform in smooth regions but wouldn't cause +10 pp jumps—those suggest trilinear "fixes" under-modeled gradients, not just snapping. Hypothesis c: grid sparsity in low-V_G2/large-dt, per log ~02:00 suggestion b, but MEP-2's denser grid (24k pts, finer spacing) doesn't address dt directly—it's a surrogate issue, not physics.

To test: Re-run the 16 cells with MEP-2's denser surrogate v3 (finer V_b to 1.0 V, reducing interp spans) using both NN and trilinear; if regressions persist in trilinear but vanish in NN, confirms b (spurious crossing discontinuities). If both improve low-V_G2 cells, points to c (sparsity).

Falsify each:
- a: Profile surrogate queries in regressed cells (e.g., d276); if NN snaps to zero Iii/Id floors unnaturally high (e.g., >1e-12 A where physics expects <1e-15 A), but trilinear smooths to physical lows, falsifies a—real physics, not artifact.
- b: Visualize interp paths across V_b for a hurt cell (d276); if trilinear outputs jump discontinuously at known physics boundaries (e.g., V_b=0.5-0.7 V, per MEP-2 non-conv at high V_b), while NN stays stable, falsifies b—numerics win.
- c: Downsample MEP-2 grid to match pre-MEP sparsity and re-test; if regressions worsen, falsifies c—not sparsity, but something like dt-surrogate mismatch.

### Q3. The V_Nwell × Iii coupling finding from MEP-3

This "striking" 2553× Iii variation (from 1.76e-13 A at V_Nwell=0.5 V to 4.50e-10 A at 5.0 V, fixed biases per mep3_summary.json diagnostic_slide21_test_point) is probably a SPICE-model artifact, not a real physics discovery worth v4.4 ink. You're overhyping it as "major" (log ~01:55) despite Id and Ileak staying constant (9.27e-7 A and ~2.78e-11 A across V_Nwell), which contradicts carrier pumping via well-body diode—real physics should modulate Id if Iii boosts body charging. The effect is suspiciously decoupled: rel_variation_iii=0.9996, but rel_variation_ileak=5.6e-7 (tiny), suggesting your diode parameterization (e.g., Js=1e-6 A/m², log ~00:55 pitfall 2) artificially amplifies Iii without downstream consistency. MEP-3's sparse 5D grid (4125 pts, conv=94.9%) has uniform conv_by_vnwell=0.949, but that's no proof—SPICE convergence masks parameterization flaws, like assuming ideal diode coupling without validated well doping.

Falsification: Ask Sebas for a single DC measurement of Iii (or proxy via body current) vs V_Nwell [0.5,5.0 V] at the exact test point (V_G1=0.4 V, V_G2=0.3 V, V_d=2.0 V, V_b=0.5 V). If silicon shows <10× variation (not 2553×), it's a model artifact—busts the "discovery."
