# Demo video

A 90-second demo recording is referenced by the paper. We avoid
checking the raw `.mp4` into git (size). To host it:

1. Upload the recording to YouTube (unlisted) or a similar host.
2. Edit this file and add the link below.

## Link

> _to be added — open an issue if you want a copy_

The demo shows:
1. `./scripts/01_collect_signature.sh` running on host A.
2. `./scripts/02_classify.sh` producing LOO accuracy 1.0.
3. `./scripts/03_test_replay.sh` showing all 7 attack gates passing.
4. `./scripts/04_demo.sh` with the browser UI rejecting a stale replay.

You can reproduce all four locally — see
[`docs/HOW_TO_REPRODUCE.md`](../docs/HOW_TO_REPRODUCE.md).
