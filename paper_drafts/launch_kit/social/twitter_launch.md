# Twitter / X launch tweet — single shot

**When:** Tuesday 14:00 UTC. **Pin after posting.**
**Media:** attach `twitter_60s.mp4` (the 60-s transplant demo).

---

## The tweet (272 chars incl. URL as t.co=23)

Two identical AMD laptops. Same SKU, same RAM, same BIOS.

Only one can run our AI — the other is rejected by the verifier.

No vendor key. No Secure Enclave. Just per-die physics.

100% LOO on N=2, 1.12 ms verify. MIT.

https://github.com/Heigke/FabricCrypt

---

### Char count check

```
Two identical AMD laptops. Same SKU, same RAM, same BIOS.\n\n
Only one can run our AI — the other is rejected by the verifier.\n\n
No vendor key. No Secure Enclave. Just per-die physics.\n\n
100% LOO on N=2, 1.12 ms verify. MIT.\n\n
https://github.com/Heigke/FabricCrypt
```

Body (excluding URL): 221 chars. URL counts as 23 on X. **X-weighted total: 244 / 280.** Safe.

---

## Video alt-text (for screen readers — required field on X)

> Two identical HP Z2 mini G1a laptops side by side. A model file is copied to chassis A: the FabricCrypt verifier returns PASS and the AI responds normally. The same file is copied to chassis B: the verifier returns REJECT and the AI refuses to run. The file is copied back to A: PASS again. Same code, same model, different silicon die.

(629 chars — fits X's 1000-char alt-text limit.)

---

## Backup variants (if the primary feels long)

**B1 — 258 chars total:**

> Same SKU. Same RAM. Same BIOS. Same microcode.
>
> Move the model to laptop B and the verifier rejects it.
> Move it back to A and it works.
>
> No vendor key. No Secure Enclave. Just per-die physics.
>
> 100% LOO, N=2, 1.12 ms.
>
> https://github.com/Heigke/FabricCrypt

**B2 — 244 chars total, sharper hook:**

> We made two identical computers. Only one can run our AI.
>
> Same SKU. Same firmware. Different silicon.
>
> No vendor PKI. Sub-ms verify. 100% LOO on N=2.
>
> Code is MIT-licensed. Bring a third chassis.
>
> https://github.com/Heigke/FabricCrypt
